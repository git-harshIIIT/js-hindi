const id = 1234
let address = "Varanasi"
var name = "Harshit"
let roll;

// id = 2345  // can't change value of a const variable
console.log(id)
console.log(roll) //undefined until its un-defined
console.table([id,address,name,roll])

// don't use var as it isn't a scope term
